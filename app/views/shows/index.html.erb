<div class="page show-list">
    <h2>Shows list</h2>
  <br>

  <%= paginate @all_shows %>

  <ul class="large-block-grid-5">
            <% @all_shows.each { |show| %>
            <li>
              <div class="main-blocks">
                <%= image_tag(show.photo, class: 'show-list') %>
              </div>
              <div class="blocks-names"><%= link_to show.name, show %></div>
            </li>
            <% } %>
          </ul>

</div>