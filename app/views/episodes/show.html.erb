<% content_for :title, @show.name + ' : ' + @episode.name %>
<div class="page">
  <div class="show-info">
    <div class="row">
    <div class="avatar large-7 columns">
      <%= image_tag @episode.photo_orig, class: 'episode-avatar' %>
    </div>
    <div class="show-text large-5 columns">
      <b><%= I18n.t 'episode.name' %>:</b> <%= @episode.name %>
      <br>
      <b><%= I18n.t 'episode.show' %>:</b> <%= link_to @show.name, @show %>
      <br>
      <b><%= I18n.t 'episode.released' %>:</b> <%= @episode.released.strftime('%d.%m.%Y') %>
      <br>
      <b><%= I18n.t 'show.rate-imdb' %>:</b>
          <% if @episode.rate_imdb == nil %>
              Not rated yet
          <% else %>
              <%= @episode.rate_imdb %>
          <% end %>
      <br>
      <b><%= I18n.t 'show.users-rate' %>:</b> <%= @episode.users_rate %>
      <br>
      <b><%= I18n.t 'episode.season' %>:</b> <%= @episode.season %>
      <br>
      <b><%= I18n.t 'episode.episode' %>:</b> <%= @episode.number %>
      <br>
      <b><%= I18n.t 'show.runtime' %>:</b> <%= @show.runtime %> min.
      <br>
      <b><%= I18n.t 'show.about' %>:</b> <%= @episode.about %>
      <br>
    </div>
    </div>
    <p><%= link_to (I18n.t 'comment.add'), new_episode_comment_path(@episode) %></p>

    <ul class="comment_list">
      <%= render :partial => 'comments/comment', :collection => @episode.comments %>
    </ul>
  </div>
</div>
