<% content_for :title, 'Genres list' %>
<div class="page">
  <div class="genres">

  <% @all_genres.each { |genre| %>
      <h6 align="center"><%= link_to genre.name, genre %></h6>
  <% } %>
  </div>
</div>
